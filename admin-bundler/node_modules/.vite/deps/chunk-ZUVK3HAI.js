import {
  useCountries,
  useCountryTableColumns,
  useCountryTableQuery
} from "./chunk-MD35TTE3.js";
import {
  ChipGroup
} from "./chunk-ULPN2T4G.js";
import {
  SplitView
} from "./chunk-EGRUNA7D.js";
import {
  DataTable,
  useDataTable
} from "./chunk-JH6E7Y6M.js";
import {
  createColumnHelper
} from "./chunk-TI6Y7EUW.js";
import {
  countries
} from "./chunk-PCCLBZD7.js";
import {
  z
} from "./chunk-D6YTPD33.js";
import {
  Form,
  useFieldArray
} from "./chunk-TKBVWLOU.js";
import {
  Button,
  Checkbox,
  useTranslation
} from "./chunk-ZUPX4YFG.js";
import {
  require_jsx_runtime
} from "./chunk-P6CFP5BP.js";
import {
  __toESM,
  require_react
} from "./chunk-H5NG3XTT.js";

// node_modules/@medusajs/dashboard/dist/chunk-ZJ3WKFSQ.mjs
var import_react = __toESM(require_react(), 1);
var import_jsx_runtime = __toESM(require_jsx_runtime(), 1);
var GeoZoneSchema = z.object({
  countries: z.array(
    z.object({ iso_2: z.string().min(2), display_name: z.string() })
  )
});
var GeoZoneFormImpl = ({
  form,
  onOpenChange
}) => {
  const castForm = form;
  const { t } = useTranslation();
  const { fields, remove, replace } = useFieldArray({
    control: castForm.control,
    name: "countries",
    keyName: "iso_2"
  });
  const handleClearAll = () => {
    replace([]);
  };
  validateForm(form);
  return (0, import_jsx_runtime.jsx)(
    Form.Field,
    {
      control: form.control,
      name: "countries",
      render: () => {
        return (0, import_jsx_runtime.jsxs)(Form.Item, { children: [
          (0, import_jsx_runtime.jsxs)("div", { className: "flex items-center justify-between gap-x-4", children: [
            (0, import_jsx_runtime.jsxs)("div", { children: [
              (0, import_jsx_runtime.jsx)(Form.Label, { children: t("stockLocations.serviceZones.manageAreas.label") }),
              (0, import_jsx_runtime.jsx)(Form.Hint, { children: t("stockLocations.serviceZones.manageAreas.hint") })
            ] }),
            (0, import_jsx_runtime.jsx)(
              Button,
              {
                size: "small",
                variant: "secondary",
                type: "button",
                onClick: () => onOpenChange(true),
                children: t("stockLocations.serviceZones.manageAreas.action")
              }
            )
          ] }),
          (0, import_jsx_runtime.jsx)(Form.ErrorMessage, {}),
          (0, import_jsx_runtime.jsx)(Form.Control, { className: "mt-0", children: fields.length > 0 && (0, import_jsx_runtime.jsx)(
            ChipGroup,
            {
              onClearAll: handleClearAll,
              onRemove: remove,
              className: "py-4",
              children: fields.map((field, index) => (0, import_jsx_runtime.jsx)(ChipGroup.Chip, { index, children: field.display_name }, field.iso_2))
            }
          ) })
        ] });
      }
    }
  );
};
var PREFIX = "ac";
var PAGE_SIZE = 50;
var AreaDrawer = ({
  form,
  open,
  onOpenChange
}) => {
  const castForm = form;
  const { t } = useTranslation();
  const { getValues, setValue } = castForm;
  const [selection, setSelection] = (0, import_react.useState)({});
  const [state, setState] = (0, import_react.useState)(
    []
  );
  const { searchParams, raw } = useCountryTableQuery({
    pageSize: PAGE_SIZE,
    prefix: PREFIX
  });
  const { countries: countries2, count } = useCountries({
    countries: countries.map((c) => ({
      display_name: c.display_name,
      name: c.name,
      iso_2: c.iso_2,
      iso_3: c.iso_3,
      num_code: c.num_code
    })),
    ...searchParams
  });
  (0, import_react.useEffect)(() => {
    if (!open) {
      return;
    }
    const countries3 = getValues("countries");
    if (countries3) {
      setState(
        countries3.map((country) => ({
          iso_2: country.iso_2,
          display_name: country.display_name
        }))
      );
      setSelection(
        countries3.reduce(
          (acc, country) => ({
            ...acc,
            [country.iso_2]: true
          }),
          {}
        )
      );
    }
  }, [open, getValues]);
  const updater = (fn) => {
    const value = typeof fn === "function" ? fn(selection) : fn;
    const ids = Object.keys(value);
    const addedIdsSet = new Set(ids.filter((id) => value[id] && !selection[id]));
    const addedCountries = [];
    if (addedIdsSet.size > 0) {
      const countriesToAdd = (countries2 == null ? void 0 : countries2.filter((country) => addedIdsSet.has(country.iso_2))) ?? [];
      for (const country of countriesToAdd) {
        addedCountries.push({
          iso_2: country.iso_2,
          display_name: country.display_name
        });
      }
    }
    setState((prev) => {
      const filteredPrev = prev.filter((country) => value[country.iso_2]);
      return Array.from(/* @__PURE__ */ new Set([...filteredPrev, ...addedCountries]));
    });
    setSelection(value);
  };
  const handleAdd = () => {
    setValue("countries", state, {
      shouldDirty: true,
      shouldTouch: true
    });
    onOpenChange(false);
  };
  const columns = useColumns();
  const { table } = useDataTable({
    data: countries2 || [],
    columns,
    count,
    enablePagination: true,
    enableRowSelection: true,
    getRowId: (row) => row.iso_2,
    pageSize: PAGE_SIZE,
    rowSelection: {
      state: selection,
      updater
    },
    prefix: PREFIX
  });
  validateForm(form);
  return (0, import_jsx_runtime.jsx)(SplitView.Drawer, { children: (0, import_jsx_runtime.jsxs)("div", { className: "flex h-full flex-col overflow-hidden", children: [
    (0, import_jsx_runtime.jsx)(
      DataTable,
      {
        table,
        columns,
        pageSize: PAGE_SIZE,
        count,
        search: true,
        pagination: true,
        layout: "fill",
        orderBy: ["name", "code"],
        queryObject: raw,
        prefix: PREFIX
      }
    ),
    (0, import_jsx_runtime.jsxs)("div", { className: "flex items-center justify-end gap-x-2 border-t p-4", children: [
      (0, import_jsx_runtime.jsx)(SplitView.Close, { type: "button", asChild: true, children: (0, import_jsx_runtime.jsx)(Button, { variant: "secondary", size: "small", children: t("actions.cancel") }) }),
      (0, import_jsx_runtime.jsx)(Button, { size: "small", type: "button", onClick: handleAdd, children: t("actions.add") })
    ] })
  ] }) });
};
var columnHelper = createColumnHelper();
var useColumns = () => {
  const base = useCountryTableColumns();
  return (0, import_react.useMemo)(
    () => [
      columnHelper.display({
        id: "select",
        header: ({ table }) => {
          return (0, import_jsx_runtime.jsx)(
            Checkbox,
            {
              checked: table.getIsSomePageRowsSelected() ? "indeterminate" : table.getIsAllPageRowsSelected(),
              onCheckedChange: (value) => table.toggleAllPageRowsSelected(!!value)
            }
          );
        },
        cell: ({ row }) => {
          const isPreselected = !row.getCanSelect();
          return (0, import_jsx_runtime.jsx)(
            Checkbox,
            {
              checked: row.getIsSelected() || isPreselected,
              disabled: isPreselected,
              onCheckedChange: (value) => row.toggleSelected(!!value),
              onClick: (e) => {
                e.stopPropagation();
              }
            }
          );
        }
      }),
      ...base
    ],
    [base]
  );
};
function validateForm(form) {
  if (form.getValues("countries") === void 0) {
    throw new Error(
      "The form does not have a field named 'countries'. This field is required to use the GeoZoneForm component."
    );
  }
}
var GeoZoneForm = Object.assign(GeoZoneFormImpl, {
  AreaDrawer
});

export {
  GeoZoneForm
};
//# sourceMappingURL=chunk-ZUVK3HAI.js.map
