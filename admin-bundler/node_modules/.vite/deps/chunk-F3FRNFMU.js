import {
  useDeleteProductType
} from "./chunk-7VADNSL7.js";
import {
  toast,
  usePrompt,
  useTranslation
} from "./chunk-ZUPX4YFG.js";

// node_modules/@medusajs/dashboard/dist/chunk-V4VFZ34C.mjs
var useDeleteProductTypeAction = (id) => {
  const { t } = useTranslation();
  const prompt = usePrompt();
  const { mutateAsync } = useDeleteProductType(id);
  const handleDelete = async () => {
    const result = await prompt({
      title: t("general.areYouSure"),
      description: t("productTypes.delete.confirmation"),
      confirmText: t("actions.delete"),
      cancelText: t("actions.cancel")
    });
    if (!result) {
      return;
    }
    await mutateAsync(void 0, {
      onSuccess: () => {
        toast.success(t("productTypes.delete.successToast"));
      },
      onError: (e) => {
        toast.error(e.message);
      }
    });
  };
  return handleDelete;
};

export {
  useDeleteProductTypeAction
};
//# sourceMappingURL=chunk-F3FRNFMU.js.map
